name: Backend Api CI

on:
  push:
    branches: [dev, main, chore/ci-cd-backend-initial-setup]
    paths:
      - "apps/api/**"
  pull_request:
    branches: [dev]
    paths:
      - "apps/api/**"

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Lint
        run: bun run --filter apps/api lint

      - name: Test
        run: bun run --filter apps/api test

      - name: Build
        run: bun run --filter apps/api build
    outputs:
      sha: ${{ github.sha }}
